export { outline }

import { create } from '@stylexjs/stylex'

const outline = create({
  hidden: {
    outlineColor: 'transparent',
    outlineOffset: '2px',
    outlineStyle: 'solid',
    outlineWidth: '2px',
  },
})

//

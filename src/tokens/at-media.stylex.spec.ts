describe('atMedia', () => {
  it('matches snapshot', () => {
    expect(atMedia).toMatchSnapshot()
  })
})

import { atMedia } from '#/tokens/at-media.stylex'
import { describe } from 'vitest'
import { expect } from 'vitest'
import { it } from 'vitest'
//

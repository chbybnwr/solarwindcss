describe('borderRadius', () => {
  it('matches snapshot', () => {
    expect(borderRadius).toMatchSnapshot()
  })
})

import { borderRadius } from '#/tokens/border-radius.stylex'
import { describe } from 'vitest'
import { expect } from 'vitest'
import { it } from 'vitest'
//

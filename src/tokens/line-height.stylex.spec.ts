describe('lineHeight', () => {
  it('matches snapshot', () => {
    expect(lineHeight).toMatchSnapshot()
  })
})

import { describe } from 'vitest'
import { expect } from 'vitest'
import { it } from 'vitest'
import { lineHeight } from '#/tokens/line-height.stylex'
//
